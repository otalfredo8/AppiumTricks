package core;

import org.apache.log4j.Logger;

//Class to output debuggin information
//For debuggin every method can have a log (VERY tedious)
public class MyLogger {
    public static Logger log = Logger.getLogger(MyLogger.class);
}
